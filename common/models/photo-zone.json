{
  "name": "PhotoZone",
  "base": "PersistedModel",
  "idInjection": true,
  "options": {
    "validateUpsert": true
  },
  "properties": {
    "createdAt": {
      "type": "date",
      "required": true,
      "default": "$now"
    },
    "modifiedAt": {
      "type": "date",
      "required": true,
      "default": "$now"
    },
    "title": {
      "type": "string"
    },
    "content": {
      "type": "string"
    }
  },
  "validations": [],
  "relations": {},
  "acls": [],
  "methods": {}
}
